<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gradient-to-r from-blue-900 via-teal-800 to-green-800 text-white min-h-screen flex flex-col items-center justify-center"
  >
    <div class="container mx-auto p-6">
      <!-- Go Back Button -->
      <a
        href="/"
        class="inline-block bg-yellow-400 hover:bg-yellow-500 text-gray-900 font-semibold px-4 py-2 rounded-md transition duration-200 shadow-lg"
      >
        ⬅️ Go Back to Home
      </a>

      <!-- Edit Task Form -->
      <div
        class="max-w-2xl mx-auto mt-10 bg-white/10 backdrop-blur-md p-6 rounded-lg shadow-xl border border-white/20"
      >
        <h1 class="text-3xl font-bold text-yellow-300">Edit Task</h1>
        <form
          action="/edit/<%= filename %>"
          method="post"
          class="space-y-4 mt-4"
        >
          <label class="block text-white/80">Previous Task Name</label>
          <input
            class="w-full p-3 bg-white/20 text-white rounded-md outline-none focus:ring-2 focus:ring-emerald-400 placeholder-white/60"
            type="text"
            name="previous"
            value="<%= filename %>"
            readonly
          />

          <label class="block text-white/80">New Task Name</label>
          <input
            class="w-full p-3 bg-white/20 text-white rounded-md outline-none focus:ring-2 focus:ring-emerald-400 placeholder-white/60"
            type="text"
            placeholder="Enter new name"
            name="new"
            required
          />

          <button
            class="w-full bg-yellow-400 hover:bg-yellow-500 text-gray-900 font-semibold py-2 px-4 rounded-md transition duration-200 shadow-lg"
          >
            ✏️ Update Name
          </button>
        </form>
      </div>
    </div>

    <script src="/javascripts/script.js"></script>
  </body>
</html>
